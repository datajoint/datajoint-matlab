function saveLocal()
    % SAVELOCAL()
    %   Description:
    %     Saves the settings in the local config file.
    %   Examples:
    %     dj.config.saveLocal
    dj.internal.Settings.saveLocal();
end